---
'@joint/jest-preset-mock-svg': minor
---

new package - Jest preset supplying the `jsdom` environment, the SVG mocks from `@joint/mock-svg`, and the `transformIgnorePatterns` exception that makes `@joint/*` transformable
