---
"@joint/core": minor
---

dia.Paper - add `startLinkDrag()` returning a `dia.LinkDrag` handle to drag an end of any link, either manually (`move()`, `finish()`, `cancel()`) or with the pointer (`followPointer()`, press-drag-release or click-move-click)
