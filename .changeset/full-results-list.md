---
"@joint/router-avoid": minor
---

RouterService - a `'done'` `RoutingResult` from `routeAll()`/`routeSubgraph()` now carries `routedLinks` (`RoutedLink[]`): the links whose route was applied, with their `origin`/`reason`
