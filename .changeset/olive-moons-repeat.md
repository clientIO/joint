---
'@joint/mock-svg': minor
---

new package - test-runner-agnostic mocks of the SVG globals JSDOM does not implement; plain functions rather than spies, so mock-reset options cannot strip them between tests
