/**
 * Unique identifier for a cell (element or link) in the graph.
 * Always a string in the React bindings.
 */
// eslint-disable-next-line sonarjs/redundant-type-aliases -- Semantic alias for readability
export type CellId = string;
