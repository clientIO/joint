import { Meta, Story, Canvas, Controls, Markdown } from '@storybook/blocks';
import * as Stories from './story';
import Code from './code?raw';


<Meta of={Stories} />

# Example showing card with children render 
Example showing card component with custom `onSetSize` method which override default behavior of measuredNode.

When measured node is used, its recommended to not define `width` and `height` in the node data itself.
### Demo
<Canvas of={Stories.Default} />

### Code
<Markdown>
{`\`\`\`tsx
${Code}
\`\`\``}
</Markdown>






