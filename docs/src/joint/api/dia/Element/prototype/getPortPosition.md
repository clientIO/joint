<pre class="docs-method-signature"><code>element.getPortPosition(portId)</code></pre>

Returns the port position as a `g.Point`, relatively to the element position. It's `undefined` until the port is rendered. 

