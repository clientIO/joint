language: node_js
node_js:
  - 16

before_install:
  - npm install -g grunt-cli
  - corepack enable

install:
  - yarn install

before_script:
  - yarn run build

script:
  - yarn run test
  - yarn run lint
