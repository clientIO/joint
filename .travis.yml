language: node_js
dist: focal
node_js:
  - 18

before_install:
  - npm install -g grunt-cli
  - corepack enable

install:
  - yarn install

before_script:
  - yarn run build

script:
  - yarn run test
  - yarn run lint
